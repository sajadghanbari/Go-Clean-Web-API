{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "swag",
            "type": "shell",
            "command": "swag",
            "args": [
                "init",
                "-g",
                "cmd/main.go",
                "--parseDependency",
                "--parseInternal"
            ],
            "options": {
                "cwd": "${workspaceFolder}/src"
            }
        }
    ]
}